{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pnpm exec eleventy --serve",
    "build:cv": "node scripts/build-cv.js",
    "build:social": "node scripts/build-social-images.js",
    "build": "pnpm exec eleventy && pnpm run build:cv && pnpm run build:social",
    "postinstall": "pnpm exec puppeteer browsers install chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.24.0",
  "dependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@11ty/eleventy-plugin-rss": "^3.0.0",
    "prismjs": "^1.30.0"
  },
  "devDependencies": {
    "@csstools/postcss-global-data": "3.1.0",
    "marked": "^18.0.4",
    "puppeteer": "^25.1.0"
  }
}
